# Optional — enables real LLM-generated answers in local dev only.
# Copy to .env, fill in your key, then run `npm run dev:llm` in a second
# terminal alongside `npm run dev`. Never used by the deployed static site —
# see server/llm-proxy.mjs for why.
GEMINI_API_KEY=
